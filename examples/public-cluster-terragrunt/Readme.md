# Public cluster with terrarunt

This example ilustrates how to use this module with `terragrunt`. It's prepared to create multiple environments (only `dev` configured now).

## Usage

Inside the `dev` directory run:
